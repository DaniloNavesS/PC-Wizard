import React from 'react'
import ReactDOM from 'react-dom/client'
import HeaderGeral from './assets/header/header'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <HeaderGeral></HeaderGeral>
  </React.StrictMode>,
)
